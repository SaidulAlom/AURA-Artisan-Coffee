# API_KEY: Required for external API calls (if used).
# Set this to your API key.
API_KEY="MY_API_KEY"

# APP_URL: The URL where this app is hosted (used for self-referential links, OAuth callbacks, and API endpoints).
APP_URL="MY_APP_URL"
